name: catshout
summary: Add your summary here
command: ["go", "run", ".."]
type: processor
cwd: "./inner"
fields:
  - name: suffix
    description: "Text to add onto the end of each message"
    type: string
    kind: scalar
    default: ", eh?"

