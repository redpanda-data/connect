name: catshout
summary: Add your summary here
command: ["go", "run", "./catshout"]
type: processor
cwd: "./.."
fields:
  - name: suffix
    description: "Text to add onto the end of each message"
    type: string
    kind: scalar
    default: ", eh?"

