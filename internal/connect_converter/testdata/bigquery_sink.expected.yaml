# Converted from Kafka Connect connector "bq" (class=com.wepay.kafka.connect.bigquery.BigQuerySinkConnector). Review # TODO markers.
input:
  redpanda:
    seed_brokers:
      - localhost:9092 # TODO: set your Redpanda/Kafka broker(s)
    topics:
      - orders
    consumer_group: connect-bq # TODO: confirm consumer group
output:
  gcp_bigquery:
    project: proj
    dataset: ds
    table: "" # TODO: set the destination table (KC derives it from the topic)
