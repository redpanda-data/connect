# Converted from Kafka Connect connector "gcs" (class=io.confluent.connect.gcs.GcsSinkConnector). Review # TODO markers.
input:
  redpanda:
    seed_brokers:
      - localhost:9092 # TODO: set your Redpanda/Kafka broker(s)
    topics:
      - orders
    consumer_group: connect-gcs # TODO: confirm consumer group
output:
  gcp_cloud_storage:
    bucket: bkt
    path: ${! @kafka_topic }/${! timestamp_unix() }-${! uuid_v4() }.json # TODO: review object path/partitioning
