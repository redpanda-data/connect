# Converted from Kafka Connect connector "sf-full" (class=com.snowflake.kafka.connector.SnowflakeSinkConnector). Review # TODO markers.
input:
  stdin: {} # TODO: set the input that feeds this pipeline (e.g. your source topic)
output:
  snowflake_streaming:
    account: myorg-myacct.snowflakecomputing.com # TODO: RPCN expects the account identifier, not the full URL
    user: loader
    role: LOADER_ROLE
    database: PROD
    schema: PUBLIC
    private_key: MYPRIVKEY
    private_key_pass: s3cr3t
    table: "" # TODO: set the destination table (KC derives it from the topic)
    batching:
      count: 10000
      byte_size: 5000000
      period: 10s
