# Converted from Kafka Connect connector "bq-full" (class=com.wepay.kafka.connect.bigquery.BigQuerySinkConnector). Review # TODO markers.
input:
  redpanda:
    seed_brokers:
      - localhost:9092 # TODO: set your Redpanda/Kafka broker(s)
    topics:
      - orders
      - events
    consumer_group: connect-bq-full # TODO: confirm consumer group
output:
  gcp_bigquery:
    project: my-project
    dataset: my_dataset
    table: ${! @kafka_topic } # TODO: table derived from the topic — verify topic-name casing rules match BigQuery naming
    credentials_json: '{"type":"service_account"}'
    create_disposition: CREATE_NEVER
    batching:
      count: 100
