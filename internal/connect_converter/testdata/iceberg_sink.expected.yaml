# Converted from Kafka Connect connector "events-sink" (class=org.apache.iceberg.connect.IcebergSinkConnector). Review # TODO markers.
input:
  redpanda:
    seed_brokers:
      - localhost:9092 # TODO: set your Redpanda/Kafka broker(s)
    topics:
      - events
    consumer_group: connect-events-sink # TODO: confirm consumer group
output:
  iceberg:
    catalog:
      url: https://polaris.example.com/api/catalog
      warehouse: s3://my-bucket/warehouse
      auth:
        oauth2:
          client_id: client123
          client_secret: secret456 # TODO: client secret is inlined — move to a secret/env-var reference
    namespace: analytics
    table: events
    storage:
      aws_s3:
        bucket: my-bucket
