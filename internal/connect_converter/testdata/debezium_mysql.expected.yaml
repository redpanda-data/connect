# Converted from Kafka Connect connector "debezium-mysql" (class=io.debezium.connector.mysql.MySqlConnector). Review # TODO markers.
input:
  mysql_cdc:
    dsn: mysqluser:mysqlpass@tcp(mysqlhost:3306)/shopdb # TODO: password is inlined — move to a secret/env-var reference
    tables:
      - shopdb.orders
      - shopdb.products
    checkpoint_cache: "" # TODO: checkpoint_cache is required — provide a cache resource name for BinLog position storage
    stream_snapshot: true # TODO: Debezium snapshot.mode=initial — verify this maps to stream_snapshot correctly
output:
  stdout: {} # TODO: set the output destination
