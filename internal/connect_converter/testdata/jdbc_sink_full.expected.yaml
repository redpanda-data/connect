# Converted from Kafka Connect connector "jdbc-sink-full" (class=io.confluent.connect.jdbc.JdbcSinkConnector). Review # TODO markers.
input:
  stdin: {} # TODO: set the input that feeds this pipeline (e.g. your source topic)
output:
  sql_insert:
    driver: postgres
    dsn: postgresql://h:5432/db # TODO: verify DSN format for the chosen driver
    table: orders
    columns:
      - id # TODO: list destination columns matching your message fields; KC pk.fields=id (pk.mode=)
    args_mapping: root = [ this.id ] # TODO: map message fields to column values
    suffix: "" # TODO: KC insert.mode=upsert; sql_insert emits a plain INSERT — write an ON CONFLICT/UPDATE suffix manually (needs pk.fields and the column list / fields.whitelist)
    batching:
      count: 500
