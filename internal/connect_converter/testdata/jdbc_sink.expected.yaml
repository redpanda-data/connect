# Converted from Kafka Connect connector "jdbc-sink" (class=io.confluent.connect.jdbc.JdbcSinkConnector). Review # TODO markers.
input:
  stdin: {} # TODO: set the input that feeds this pipeline (e.g. your source topic)
output:
  sql_insert:
    driver: mysql
    dsn: mysql://h:3306/db # TODO: verify DSN format for the chosen driver
    table: orders
    columns:
      - id # TODO: list destination columns matching your message fields
    args_mapping: root = [ this.id ] # TODO: map message fields to column values
