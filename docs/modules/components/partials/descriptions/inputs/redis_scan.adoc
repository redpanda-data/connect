// This content is autogenerated from the component specs in https://github.com/redpanda-data/connect. Do not edit manually. The summary and description come from the component spec. To change them, edit the spec and run `task docs` in that repository.
//
// Pages consume this partial in two places so both stay fresh:
//   header: include::...[tag=meta]  (sets :description: for search snippets and meta tags)
//   body:   include::...[tag=body]   (renders the summary and description)

// tag::meta[]
:description: Scans the set of keys in the current selected database and gets their values, using the Scan and Get commands.
// end::meta[]

// tag::body[]
Scans the set of keys in the current selected database and gets their values, using the Scan and Get commands.

ifndef::env-cloud[]
Introduced in version 4.27.0.
endif::[]

Optionally, iterates only elements matching a blob-style pattern. For example:

- `*foo*` iterates only keys which contain `foo` in it.
- `foo*` iterates only keys starting with `foo`.

This input generates a message for each key value pair in the following format:

```json
{"key":"foo","value":"bar"}
```
// end::body[]
