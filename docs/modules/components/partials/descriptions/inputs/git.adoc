// This content is autogenerated from the component specs in https://github.com/redpanda-data/connect. Do not edit manually. The summary and description come from the component spec. To change them, edit the spec and run `task docs` in that repository.
//
// Pages consume this partial in two places so both stay fresh:
//   header: include::...[tag=meta]  (sets :description: for search snippets and meta tags)
//   body:   include::...[tag=body]   (renders the summary and description)

// tag::meta[]
:description: A Git input that clones (or pulls) a repository and reads the repository contents.
// end::meta[]

// tag::body[]
A Git input that clones (or pulls) a repository and reads the repository contents.

ifndef::env-cloud[]
Introduced in version 4.51.0.
endif::[]

The git input clones the specified repository (or pulls updates if already cloned) and reads 
the content of the specified file. It periodically polls the repository for new commits and emits 
a message when changes are detected.

include::connect:components:partial$metadata/inputs/git.adoc[]
// end::body[]
