// This content is autogenerated from the component specs in https://github.com/redpanda-data/connect. Do not edit manually. The summary and description come from the component spec. To change them, edit the spec and run `task docs` in that repository.
//
// Pages consume this partial in two places so both stay fresh:
//   header: include::...[tag=meta]  (sets :description: for search snippets and meta tags)
//   body:   include::...[tag=body]   (renders the summary and description)

// tag::meta[]
:description: Unarchives messages according to the selected archive format into multiple messages within a batch.
// end::meta[]

// tag::body[]
Unarchives messages according to the selected archive format into multiple messages within a xref:configuration:batching.adoc[batch].

When a message is unarchived the new messages replace the original message in the batch. Messages that are selected but fail to unarchive (invalid format) will remain unchanged in the message batch but will be flagged as having failed, allowing you to xref:configuration:error_handling.adoc[error handle them].

Unarchived messages are kept in the same batch. To process each unarchived message individually, follow this processor with a xref:components:processors/split.adoc[`split` processor].

include::connect:components:partial$metadata/processors/unarchive.adoc[]
// end::body[]
