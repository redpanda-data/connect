// This content is autogenerated from the component specs in https://github.com/redpanda-data/connect. Do not edit manually. The summary and description come from the component spec. To change them, edit the spec and run `task docs` in that repository.
//
// Pages consume this partial in two places so both stay fresh:
//   header: include::...[tag=meta]  (sets :description: for search snippets and meta tags)
//   body:   include::...[tag=body]   (renders the summary and description)

// tag::meta[]
:description: Send OpenTelemetry traces, logs, and metrics via OTLP/gRPC protocol.
// end::meta[]

// tag::body[]
Send OpenTelemetry traces, logs, and metrics via OTLP/gRPC protocol.

ifndef::env-cloud[]
Introduced in version 4.78.0.
endif::[]

Sends OpenTelemetry telemetry data to a remote collector via OTLP/gRPC protocol.

Accepts batches of Redpanda OTEL v1 messages (spans, log records, or metrics) and converts them to OTLP format for transmission to OpenTelemetry collectors.

== Input format

This output expects messages in Redpanda OTEL v1 format (protobuf or JSON, detected automatically) with the following metadata:

- `otel_signal_type`: The signal type (`trace`, `log`, or `metric`)

Messages produced by the `otlp_grpc` and `otlp_http` inputs already include this metadata.

Each batch must contain messages of the same signal type. The entire batch is converted to a single OTLP export request and sent via gRPC.

== Authentication

This output supports OAuth v2 authentication using the `oauth2` configuration block. OAuth v2 requires TLS to be enabled.

To send a static bearer token, add an `authorization` header using the `headers` field.
// end::body[]
