// This content is autogenerated from the component specs in https://github.com/redpanda-data/connect. Do not edit manually. To change it, edit the component spec and run `task docs` in that repository.

== Fields

=== `api_key`

Your API key for the Cohere API.

include::connect:components:partial$secret_warning.adoc[]

*Type*: `string`

=== `base_url`

The base URL to use for API requests.

*Type*: `string`

*Default*: `https://api.cohere.com`

=== `documents`

A list of text strings that are compared to the specified query. For optimal performance, Cohere recommends that you:

- Send fewer than 1000 documents in a single request.
- Send structured data in YAML format.

*Type*: `string`

=== `max_tokens_per_doc`

This processor automatically truncates long documents to the specified number of tokens.

*Type*: `int`

*Default*: `4096`

=== `model`

The name of the Cohere model you want to use.

*Type*: `string`

[source,yaml]
----
# Examples:
model: rerank-v3.5
----

=== `query`

The search query you want to execute.

This field supports xref:configuration:interpolation.adoc#bloblang-queries[interpolation functions].

*Type*: `string`

=== `top_n`

The number of documents to return when the query is executed. If set to `0`, all documents are returned.

This field supports xref:configuration:interpolation.adoc#bloblang-queries[interpolation functions].

*Type*: `string`

*Default*: `0`


