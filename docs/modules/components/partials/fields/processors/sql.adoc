// This content is autogenerated from the component specs in https://github.com/redpanda-data/connect. Do not edit manually. To change it, edit the component spec and run `task docs` in that repository.

== Fields

=== `args_mapping`

An optional xref:guides:bloblang/about.adoc[Bloblang mapping] that includes the same number of values in an array as the placeholder arguments in the `query` field.

*Type*: `string`

[source,yaml]
----
# Examples:
args_mapping: root = [ this.cat.meow, this.doc.woofs[0] ]

# ---

args_mapping: root = [ meta("user.id") ]
----

=== `data_source_name`

Data source name.

*Type*: `string`

=== `driver`

A database <<drivers, driver>> to use.

*Type*: `string`

*Options*: `mysql`, `postgres`, `pgx`, `clickhouse`, `mssql`, `sqlite`, `oracle`, `snowflake`, `trino`, `gocosmos`, `spanner`, `databricks`

=== `query`

The query to execute.

You must include the correct placeholders for the specified database driver. Some drivers use question marks (`?`), whereas others expect incrementing dollar signs (`$1`, `$2`, and so on) or colons (`:1`, `:2`, and so on). The following table shows the placeholder style for each driver:

|===
| Driver | Placeholder style

| `clickhouse`
| Dollar sign (`$`)

| `gocosmos`
| Colon (`:`)

| `mssql`
| Question mark (`?`)

| `mysql`
| Question mark (`?`)

| `oracle`
| Colon (`:`)

| `pgx`
| Dollar sign (`$`)

| `postgres`
| Dollar sign (`$`)

| `snowflake`
| Question mark (`?`)

| `spanner`
| Question mark (`?`)

| `sqlite`
| Question mark (`?`)

| `trino`
| Question mark (`?`)
|===

*Type*: `string`

[source,yaml]
----
# Examples:
query: INSERT INTO footable (foo, bar, baz) VALUES (?, ?, ?);
----

=== `result_codec`

Result codec.

*Type*: `string`

*Default*: `none`

=== `unsafe_dynamic_query`

Whether to enable xref:configuration:interpolation.adoc#bloblang-queries[interpolation functions] in the query. Make sure your queries are defended against injection attacks.

*Type*: `bool`

*Default*: `false`


